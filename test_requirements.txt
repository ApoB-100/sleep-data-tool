# Test Requirements for Fitbit Sleep Tool Test Suite
# Install with: pip install -r test_requirements.txt

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0
pytest-mock>=3.11.0

# Application dependencies (needed for imports)
requests>=2.31.0
pandas>=2.0.0
pydantic>=2.0.0
requests-oauthlib>=1.3.1
cryptography>=41.0.0
python-dotenv>=1.0.0

# Optional but recommended for keyring tests
keyring>=24.0.0

# GUI testing (optional - comment out if not testing GUI)
# Note: ttkbootstrap requires tkinter which comes with Python
# ttkbootstrap>=1.10.1
